{
  "name": "lofa-web-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LOFA 移动 Web 测试: Vitest 单测(reducer/renderer) + ajv 契约 + Playwright 流程",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "ajv": "^8.17.1",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
